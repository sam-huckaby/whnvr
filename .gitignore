# Obviously, don't commit build artifacts
_build

# Don't keep bundled JS files in the repo, since they change
www/styles/*.dist.js

# Don't commit the tailwind executable
tailwindcss

# Don't commit my .env
.env

# Don't keep node_modules
node_modules
